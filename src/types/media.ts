export type MediaStatus='importing'|'analyzing'|'ready'|'error';export type AnalysisStatus='not-analyzed'|'queued'|'extracting-audio'|'transcribing'|'detecting-scenes'|'analyzing-audio'|'summarizing'|'ready'|'error';
export interface WaveformData{mediaId:string;sampleCount:number;peaks:number[];duration:number}export interface TranscriptWord{text:string;start:number;end:number;confidence?:number}export interface TranscriptSegment{id:string;start:number;end:number;text:string;speakerId?:string;words?:TranscriptWord[]}export interface TranscriptResult{mediaId:string;language?:string;duration:number;segments:TranscriptSegment[];fullText:string;provider:string;createdAt:string}export interface SilenceRange{start:number;end:number;duration:number}export interface SceneSegment{id:string;mediaId:string;start:number;end:number;duration:number;thumbnailPath?:string;index:number}export interface AudioAnalysis{hasAudio:boolean;sampleRate?:number;channels?:number;averageDb?:number;peakDb?:number;silenceRanges:SilenceRange[];contentType?:'speech'|'music'|'mixed'|'silent'|'unknown'}export interface ClipSummary{summary:string;topics:string[];contentType:string}export interface MediaAnalysis{mediaId:string;transcript?:TranscriptResult;scenes:SceneSegment[];audio?:AudioAnalysis;waveform?:WaveformData;summary?:ClipSummary;analyzedAt:string;version:number;stageErrors?:Record<string,string>}
export interface MediaItem{id:string;name:string;path:string;playbackPath?:string;duration:number;width:number;height:number;fps:number;videoCodec?:string;audioCodec?:string;hasAudio:boolean;audioChannels?:number;audioSampleRate?:number;fileSize:number;thumbnailPath?:string;createdAt?:string;status:MediaStatus;error?:string;recovered?:boolean;analysisStatus?:AnalysisStatus}
export interface ImportError{path:string;message:string}export interface ImportResult{items:MediaItem[];errors:ImportError[];canceled?:boolean}export interface ImportProgress{active:boolean;current:number;total:number;message:string}export interface AnalysisProgress{mediaId:string;status:AnalysisStatus;message:string;analysis?:MediaAnalysis}export interface MediaEngineStatus{ffmpegAvailable:boolean;ffprobeAvailable:boolean;ffmpegPath?:string;ffprobePath?:string;ffmpegVersion?:string;ffprobeVersion?:string;source?:string}export interface AppSettings{ffmpegPath:string;ffprobePath:string;transcriptionProvider:'openai'|'none';sceneThreshold:number;silenceThresholdDb:number;silenceMinimumSeconds:number;openAIConfigured?:boolean;mediaEngine?:MediaEngineStatus}export interface PersistedProject{projectName:string;mediaItems:MediaItem[];selectedMediaId?:string;timeline?:import('./timeline').TimelineModel;projectFormat?:import('./timeline').ProjectFormat;aiEditHistory?:import('./timeline').AIEditHistoryItem[];creative?:Partial<import('./creative').CreativeState>;renderSettings?:Partial<import('./creative').ExportSettings>}
export interface EditorAPI{importMedia():Promise<ImportResult>;importFolder():Promise<ImportResult>;importPaths(paths:string[]):Promise<ImportResult>;getMediaUrl(id:string,kind?:'video'|'thumbnail'|'scene'):string;getPathForFile(file:File):string;getMediaEngineStatus():Promise<MediaEngineStatus>;loadProject():Promise<PersistedProject>;openProject():Promise<PersistedProject|null>;saveProject(project:PersistedProject):Promise<void>;saveProjectAs(project:PersistedProject):Promise<string|null>;onImportProgress(callback:(progress:ImportProgress)=>void):()=>void;startAnalysis(ids:string[]):Promise<{queued:number}>;analyzeAll():Promise<{queued:number}>;cancelAnalysis(id:string):Promise<void>;getAnalysis(id:string):Promise<MediaAnalysis|null>;onAnalysisProgress(callback:(progress:AnalysisProgress)=>void):()=>void;getSettings():Promise<AppSettings>;saveSettings(value:AppSettings):Promise<AppSettings>;browseEngine(kind:'ffmpeg'|'ffprobe'):Promise<string>;generateEdit(input:{prompt:string;mode:import('./timeline').EditingMode;targetDuration:number;aspectRatio:import('./timeline').AspectRatio;projectName:string}):Promise<import('./timeline').EditPlan>;validateEditPlan(plan:import('./timeline').EditPlan):Promise<import('./timeline').EditPlan>;startExport(request:{composition:import('../rendering/remotion/compositionAdapter').SavanaCompositionProps;settings:import('./creative').ExportSettings}):Promise<{cancelled?:boolean;outputPath?:string}>;cancelExport():Promise<boolean>;onExportProgress(callback:(value:import('./creative').ExportProgress)=>void):()=>void;openExternal(url:string):Promise<void>;showAbout():Promise<void>;exitApp():Promise<void>}
declare global{interface Window{editorAPI:EditorAPI}}
