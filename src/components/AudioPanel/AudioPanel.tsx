import{useTimelineStore}from'../../stores/timelineStore';
export function AudioPanel(){const store=useTimelineStore(),clip=store.timeline.tracks.flatMap(track=>track.clips).find(item=>item.id===store.selectedClipIds[0]);const update=(patch:Record<string,unknown>)=>{if(!clip)return;const next=structuredClone(store.timeline),target=next.tracks.flatMap(track=>track.clips).find(item=>item.id===clip.id);if(!target)return;target.audio={volume:1,muted:false,fadeIn:0,fadeOut:0,gainDb:0,pan:0,normalize:false,...target.audio,...patch};useTimelineStore.setState({timeline:next,past:[...store.past.slice(-49),structuredClone(store.timeline)],future:[]})};return <section className="media-browser inspector"><div className="panel-title">Audio</div>{!clip?<div className="empty-state"><p>Select a clip to adjust audio.</p></div>:<>{[['Volume','volume',0,2,.05],['Gain (dB)','gainDb',-24,24,.5],['Pan','pan',-1,1,.05],['Fade In','fadeIn',0,10,.1],['Fade Out','fadeOut',0,10,.1]].map(([label,key,min,max,step])=><label key={String(key)}><span>{label}</span><input type="number" min={min} max={max} step={step} value={Number(clip.audio?.[key as keyof typeof clip.audio]??(key==='volume'?1:0))} onChange={e=>update({[String(key)]:Number(e.target.value)})}/></label>)}<label><span>Muted</span><input type="checkbox" checked={clip.audio?.muted||false} onChange={e=>update({muted:e.target.checked})}/></label><p className="panel-note">EQ, compression, normalization, ducking, and beat analysis remain unavailable until the configured FFmpeg engine can be validated.</p></>}</section>}
