import{create}from'zustand';import type{ImportProgress,MediaAnalysis,MediaItem,PersistedProject}from'../types/media';export type EditorPanel='media'|'assistant'|'transcripts'|'effects'|'transitions'|'audio'|'inspector';export type MediaViewMode='grid'|'list';
interface EditorState{mediaItems:MediaItem[];analysisById:Record<string,MediaAnalysis>;selectedMediaId?:string;projectName:string;activePanel:EditorPanel;mediaViewMode:MediaViewMode;importProgress:ImportProgress;setActivePanel:(p:EditorPanel)=>void;setProjectName:(n:string)=>void;addMedia:(i:MediaItem)=>void;addMediaBatch:(i:MediaItem[])=>void;updateMedia:(id:string,p:Partial<MediaItem>)=>void;setAnalysis:(id:string,a:MediaAnalysis)=>void;removeMedia:(id:string)=>void;selectMedia:(id?:string)=>void;clearMedia:()=>void;setMediaViewMode:(m:MediaViewMode)=>void;setImportProgress:(p:ImportProgress)=>void;hydrate:(p:PersistedProject)=>void}
const dedupe=(current:MediaItem[],incoming:MediaItem[])=>{const paths=new Set(current.map(x=>x.path.toLocaleLowerCase()));return[...current,...incoming.filter(x=>!paths.has(x.path.toLocaleLowerCase()))]};
export const useEditorStore=create<EditorState>(set=>({mediaItems:[],analysisById:{},projectName:'Untitled Project',activePanel:'media',mediaViewMode:'grid',importProgress:{active:false,current:0,total:0,message:'Ready'},setActivePanel:activePanel=>set({activePanel}),setProjectName:projectName=>set({projectName}),addMedia:item=>set(s=>({mediaItems:dedupe(s.mediaItems,[item])})),addMediaBatch:items=>set(s=>({mediaItems:dedupe(s.mediaItems,items)})),updateMedia:(id,patch)=>set(s=>({mediaItems:s.mediaItems.map(x=>x.id===id?{...x,...patch}:x)})),setAnalysis:(id,value)=>set(s=>({analysisById:{...s.analysisById,[id]:value}})),removeMedia:id=>set(s=>({mediaItems:s.mediaItems.filter(x=>x.id!==id),selectedMediaId:s.selectedMediaId===id?undefined:s.selectedMediaId})),selectMedia:selectedMediaId=>set({selectedMediaId}),clearMedia:()=>set({mediaItems:[],selectedMediaId:undefined}),setMediaViewMode:mediaViewMode=>set({mediaViewMode}),setImportProgress:importProgress=>set({importProgress}),hydrate:p=>set({projectName:p.projectName||'Untitled Project',mediaItems:p.mediaItems||[],selectedMediaId:p.selectedMediaId})}));
